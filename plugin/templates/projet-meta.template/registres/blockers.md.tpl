# Blockers — {{PROJET_NAME}}

> Problems encountered and their resolutions (or "open" status if unresolved).
> Format: `## B<NN> — <short title>` then description, cause, solution, status.

> Note: candidate blockers automatically detected by the PostToolUse hook first land in `.claude/pending-blockers.md` and are triaged in `/cloture-session`.

---

(Empty — to fill in as you go)
