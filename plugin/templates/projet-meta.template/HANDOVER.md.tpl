# HANDOVER — {{PROJET_NAME}} (meta-project)

> Preloaded at S0. To update at the end of each session.
> Parent lab: `{{LAB_NAME}}` — `{{LAB_PATH_RELATIVE}}/`
> Deliverable: `{{LIVRABLE_PATH_RELATIVE}}/`
> Created: {{PROJET_CREATED}}

## Final state of the session
(N/A — no session yet)

## What has been done
- Meta-project bootstrap via the `/new-projet` workflow
- Deliverable folder created at `{{LIVRABLE_PATH_RELATIVE}}/` (README + .gitignore)
- Affiliation to the lab `{{LAB_NAME}}` (via this `projets-meta/` location)

## What remains to be done (next session)
- Discovery + PRD via `/business-analyst`
- Architecture via `/architecte`
- R001 coverage audit via `/reviewer-prd`
- Launch the S0 build session (the lead writes in `{{LIVRABLE_PATH_RELATIVE}}/` for the code)

## Architectural decisions (ADR)
(Empty)

## Files to read first at the start of each session
1. This HANDOVER (`./HANDOVER.md`)
2. `./CLAUDE.md` (meta-project constitution)
3. `{{LAB_PATH_RELATIVE}}/CLAUDE.md` (parent lab — via additionalDirectories)
4. `{{VIBEFORGE_PATH_RELATIVE}}/CLAUDE.md` (parent method)
5. `./docs/PRD.md` (when created)
6. `./.claude/rules/00-project-context.md`

## Technical stack (deliverable)
(To lock via `/architecte` then `./.claude/rules/01-stack-locked.md`)
