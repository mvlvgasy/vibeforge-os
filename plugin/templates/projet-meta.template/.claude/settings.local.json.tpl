{
  "_comment": "Local settings for meta-project {{PROJET_NAME}}. Gitignored. Grants access to the parent lab and the deliverable folder via additionalDirectories. The method itself (doctrine, rules, agents) comes from the installed plugin, no path reference needed.",
  "additionalDirectories": [
    "{{LAB_PATH_RELATIVE}}",
    "{{LIVRABLE_PATH_RELATIVE}}"
  ],
  "env": {}
}
